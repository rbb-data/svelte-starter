{
  "name": "Slider",
  "description": "`Slider` renders a single component at a time and provides the necessary functionality\nto navigate back and forth through swipe gestures (on touch devices) or mouse clicks\n(on desktop).\n\n`Slider` keeps track of the active slide internally. Binding to this prop (`activeIndex`)\nis a common pattern.\n\n`prev` and `next` determine the index of the previous and next slide;\nreturning `null` means there is no slide to go to.",
  "props": [
    {
      "name": "slides",
      "description": "list of slides",
      "type": "Array<any>",
      "default": null
    },
    {
      "name": "prev",
      "description": "function that determines the previous slide",
      "type": "(index: number) => number",
      "default": null
    },
    {
      "name": "next",
      "description": "function that determines the next slide",
      "type": "(index: number) => number",
      "default": null
    },
    {
      "name": "initialActiveIndex",
      "description": "intial slide index",
      "type": "number",
      "default": "0"
    },
    {
      "name": "activeIndex",
      "description": "index of the currently visible slide",
      "type": "number",
      "default": "initialActiveIndex"
    }
  ]
}