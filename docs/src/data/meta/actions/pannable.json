[
  {
    "name": "pannable",
    "description": "Makes an element pannable",
    "type": "(node: HTMLElement | SVGElement, options?: { ignorePointers: PointerType[]; }) => { destroy(): void; }",
    "params": [
      {
        "name": "node",
        "type": "HTMLElement | SVGElement",
        "description": "the element to make pannable",
        "optional": false,
        "default": null
      },
      {
        "name": "options",
        "type": "{ ignorePointers: PointerType[]; }",
        "description": "options where\n`ignorePointes` is a list of pointer types to ignore",
        "optional": true,
        "default": "defaultOptions"
      }
    ]
  },
  {
    "name": "drag",
    "description": "Creates a function that updates an element's coordinates according to the\ninformation exposed by some custom event",
    "type": "(coords: import('svelte/store').Writable<number | {    x: number;    y: number;}>, options?: {    axis?: 'xy' | 'x' | 'y';    bounds?: {        top?: number;        right?: number;        bottom?: number;        left?: number;    };}) => (event: CustomEvent<{    dx: number;    dy: number;}>) => void",
    "params": [
      {
        "name": "coords",
        "type": "Writable<number | { x: number; y: number; }>",
        "description": "a writable store that exposes an element's position",
        "optional": false,
        "default": null
      },
      {
        "name": "options",
        "type": "{ axis?: \"xy\" | \"x\" | \"y\"; bounds?: { top?: number; right?: number; bottom?: number; left?: number; }; }",
        "description": "drag options where `axis` is the axis to drag\nalong and `bounds` - if given - restricts the element to move within the\ngiven bounds",
        "optional": true,
        "default": "{ axis: 'xy' }"
      }
    ]
  }
]